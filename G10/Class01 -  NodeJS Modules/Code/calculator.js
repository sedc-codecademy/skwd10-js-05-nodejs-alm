const sumNumbers = (numberOne, numberTwo) => {
  return numberOne + numberTwo;
};

const divideNumbers = (numberOne, numberTwo) => {
  return numberOne / numberTwo;
};

// module.exports = sumNumbers;

// module.exports = {
//   sumNumbers: sumNumbers,
//   divideNumbers: divideNumbers,
// };

// very same as the one above =)

module.exports = {
  sumNumbers,
  divideNumbers,
};
